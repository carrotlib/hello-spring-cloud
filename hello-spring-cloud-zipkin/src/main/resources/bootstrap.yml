spring:
  application:
    name: hello-spring-cloud-zipkin

server:
  port: 8400

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/

# Zipkin 配置
management:
  metrics:
    web:
      server:
        auto-time-requests: false

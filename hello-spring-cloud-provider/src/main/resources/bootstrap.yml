spring:
  application:
    # 服务名
    name: hello-spring-cloud-provider

server:
  # 端口号
  port: 8100

eureka:
  client:
    serviceUrl:
      #  指定 Eureka 服务端
      defaultZone: http://localhost:8000/eureka/
